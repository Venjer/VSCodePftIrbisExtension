# IRBIS PFT Language Extension для VS Code

Расширение VS Code для поддержки языка форматирования PFT (Print Format Table) системы ИРБИС с поддержкой функций UNIFOR и встроенных функций верхнего уровня.

## Возможности

### 🚀 Автодополнение кода
- **Библиотека функций UNIFOR** — около 80 функций с документацией
- **Встроенные функции** — `&s`, `&val`, `&f`, `&rsum`, `&rmax`, `&rmin`, `&ravr`, `&l`, `&ref`
- **Ссылки на поля** — автодополнение для полей RUSMARC из встроенного справочника
- **Сниппеты** — быстрая вставка часто используемых конструкций

### 📖 Интеллектуальная документация
- **Hover-подсказки** для функций UNIFOR и встроенных функций
- **Информация о полях и подполях** при наведении на ссылки вида `v200^a`
- **Категоризация функций** по назначению

### 🎨 Подсветка синтаксиса
- Выделение функций UNIFOR (`&uf`, `&unifor`) и встроенных функций
- Подсветка ссылок на поля (`v200^a`) и глобальных переменных
- Выделение строковых литералов, комментариев и проверок наличия `p()`/`a()`
- Подсветка подключаемых форматов (`@name`)

### 🔍 Навигация и диагностика
- Переход к подключаемому формату (`@name` → `name.pft`) по F12
- Проверка парности `if`/`fi` в реальном времени
- Проверка незакрытых скобок и строковых литералов

### 📐 Форматирование
- Расстановка отступов по вложенности `if`/`fi` и групп в скобках
- Содержимое литералов и комментариев не изменяется

### ⚡ Команды и горячие клавиши
- **Ctrl+Shift+U** — показать функции по категориям
- **Ctrl+Shift+I** — форматировать документ

## Категории функций

### 📝 Работа со строками
- `uf('+95')` — длина строки
- `uf('+96')` — извлечение части строки
- `uf('Q')` / `uf('+97')` — нижний / верхний регистр
- `uf('+98')` — замена символа
- `uf('+9I')` / `uf('+9S')` — замена и поиск подстроки
- `uf('G')` — часть строки до/после разделителя
- `uf('T')` — транслитерация, `uf('X')` — удаление HTML-тегов
- `uf('=')` — сравнение с маской (`?`, `*`)
- `uf('+9R')` / `uf('+9X')` — римские и арабские числа

### 🔤 Работа со словами
- `uf('E')` — первые N слов
- `uf('F')` — слова после N-го
- `uf('+9G')` — разбиение на слова

### 📅 Дата и время
Код функции — `3`, далее код компонента:
- `uf('300')` — год, `uf('301')` — месяц, `uf('302')` — день
- `uf('303')` — день года, `uf('307')` — день недели, `uf('310')` — век
- `uf('31')` — время ЧЧММСС
- `uf('3')` без кода — полная дата ГГГГММДД

### 🌐 Глобальные переменные
- `uf('+7')` — очистить все переменные
- `uf('+7R')` / `uf('+7W')` / `uf('+7U')` — чтение, запись, добавление повторений
- `uf('+7T')` / `uf('+7G')` — сортировка и удаление дублей
- `uf('+7A')` / `uf('+7M')` / `uf('+7S')` — объединение, пересечение, вычитание
- `uf('+1R')` / `uf('+1W')` / `uf('+1G')` — тот же набор переменных, ограниченный набор операций

### 🗃️ База данных
- `uf('0')` / `uf('+0')` / `uf('+@')` — содержимое записи в разных представлениях
- `uf('A')` — заданное повторение поля, `uf('+N')` — количество повторений
- `uf('K')` — раскодировка через MNU-справочник, `uf('I')` — параметр INI-профиля
- `uf('D')` / `uf('7')` — поиск по термину с форматированием
- `uf('J')` — число вхождений термина, `uf('2')` — следующий MFN

### 📁 Файлы
- `uf('+91')` / `uf('+92')` / `uf('+93')` — имя, каталог, расширение файла
- `uf('+9C')` — вставка данных из файла, `uf('+9L')` — проверка наличия файла
- `uf('+3E')` / `uf('+3D')` — URL-кодирование и декодирование

### 🔧 Встроенные функции верхнего уровня
- `&s(...)` — вычислить подформат
- `&val(...)` — числовое значение строки
- `&f(выражение,ширина,знаков)` — форматирование числа
- `&rsum` / `&rmax` / `&rmin` / `&ravr` — агрегация чисел подформата
- `&l(термин)` — MFN записи по термину словаря
- `&ref(mfn,формат)` — применить формат к другой записи

## Установка

1. Откройте Visual Studio Code
2. Перейдите в раздел расширений (Ctrl+Shift+X)
3. Найдите "Irbis Language Support"
4. Нажмите "Установить"

### Альтернативная установка из VSIX

1. Скачайте файл `.vsix`
2. Откройте VS Code
3. Нажмите Ctrl+Shift+P
4. Введите "Extensions: Install from VSIX"
5. Выберите скачанный файл

## Использование

1. Создайте файл с расширением `.pft`
2. Начните вводить код — автодополнение активируется автоматически
3. Наведите курсор на функцию или ссылку на поле для просмотра документации
4. Используйте Ctrl+Shift+I для форматирования кода

### Пример кода

```pft
/* Комментарий действует до конца строки, закрывающего "*/" в PFT нет
if p(v200) then
    v200^a,
    if &uf('+95', v200^a) > '50' then
        ' (длинное заглавие)'
    fi
fi
```

Полный пример — в файле `example.pft`.

## Требования

- Visual Studio Code версии 1.96.0 или выше
- Нет дополнительных зависимостей

## Конфигурация

В настоящее время расширение не требует дополнительной настройки и работает "из коробки".

## Известные проблемы

- Не поддерживается автодополнение пользовательских функций
- Встроенный справочник полей RUSMARC неполный — hover работает не для всех меток
- Диагностика ограничена парностью конструкций; семантические ошибки не выявляются

## Архитектура

Исходный код организован по слоям:

```
src/
  domain/          язык PFT: токенизатор, форматтер, диагностика, справочники функций и полей
  application/      сценарии: автодополнение, hover, переход по @include, выбор функции
  infrastructure/vscode/   адаптеры к API VS Code и точка активации
```

Ключевое правило: **`domain` и `application` не импортируют `vscode`**. Весь контакт с API
редактора сосредоточен в `infrastructure/vscode/`. Это позволяет тестировать язык, форматтер,
диагностику и справочники функций обычными mocha-тестами без запуска редактора; правило
проверяется автоматически тестом `src/test/architecture.test.ts`.

## Разработка

```
npm install       # установка зависимостей
npm run compile   # сборка в out/
npm run watch     # сборка в фоновом режиме при изменениях
npm test          # компиляция + запуск тестов (mocha)
npm run package   # сборка .vsix через @vscode/vsce
```

Для отладки в самом VS Code — открыть проект и нажать F5 (Extension Development Host).

## История версий

### 0.1.0 (Текущая версия)
- Исходный код реорганизован по слоям `domain`/`application`/`infrastructure` с независимым от `vscode` доменным и прикладным слоем (см. раздел «Архитектура»)
- Сборка выводится в `out/`, пакет `.vsix` больше не содержит исходники TypeScript, тесты и служебные файлы
- Значок расширения заменён на PNG 512×512 (требование Marketplace)
- Исправлена обработка комментариев: `/*` действует до конца строки, конструкции `*/` и `//` в PFT нет
- Список ключевых слов приведён к реальному синтаксису PFT (`if/then/else/fi/and/or/not/break`); `case`, `for`, `while`, `select` удалены как несуществующие
- Библиотека UNIFOR расширена до ~80 функций, исправлены коды даты и времени и описание счётчика `S`
- Добавлены встроенные функции верхнего уровня: `&s`, `&val`, `&f`, `&rsum`, `&rmax`, `&rmin`, `&ravr`, `&l`, `&ref`
- Добавлены hover-подсказки по полям и подполям RUSMARC
- Добавлен переход к подключаемому формату (`@name`) по F12
- Добавлена диагностика парности `if`/`fi`, скобок и строковых литералов
- Форматирование переписано: отступы по вложенности, содержимое литералов и комментариев не изменяется
- Удалён дублирующий legacy-список функций, добавлены модульные тесты (63, включая проверку архитектурных границ)

### 0.0.3
- Добавлена поддержка hover-подсказок
- Улучшено автодополнение функций
- Добавлены snippet'ы для часто используемых функций
- Исправлены ошибки в подсветке синтаксиса

### 0.0.2
- Добавлено автодополнение встроенных функций
- Улучшена подсветка синтаксиса
- Добавлено форматирование кода

### 0.0.1
- Первоначальный релиз
- Базовая подсветка синтаксиса для .pft файлов

## Обратная связь

Если у вас есть предложения по улучшению или вы нашли ошибку, пожалуйста, создайте issue в репозитории проекта.

## Лицензия

Данное расширение распространяется под лицензией MIT.

---

**Приятного использования!** 🚀
